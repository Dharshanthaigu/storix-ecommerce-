# Known Issues & Fixes

| # | File | Error | Fix |
|---|------|-------|-----|
| 1 | `models/Ticket.ts` | `MissingSchemaError: Schema hasn't been registered for model "user"` | `ref: "user"` -> `ref: "User"` in the `Ticket` schema's `user` field |
| 2 | `orderController.ts`, `ticketController.ts`, `productController.ts`, `categoryController.ts` | Mongoose deprecation warning: `the 'new' option is deprecated` | In every `findByIdAndUpdate(id, data, { new: true })` call, changed the options object to `{ returnDocument: "after" }` |
| 3 | `models/Order.ts` | `idempotencyKey` schema had conflicting `required: true` + `sparse: true` | In the `idempotencyKey` field definition, changed `required: true` to `required: false` (kept `unique: true, sparse: true`) |
| 4 | `models/Order.ts` | `couponCode`/`discountAmount`/`razorpayOrderId` silently dropped on save | Added `couponCode?: string;` and `discountAmount?: number;` to the `IOrder` interface, and `couponCode: { type: String }`, `discountAmount: { type: Number, default: 0 }` to `OrderSchema` |
| 5 | `addressController.ts` | `No overload matches this call... '_id' does not exist in type Query<...>` | In `updateAddress`: `Address.findByIdAndUpdate({ _id: req.params.id, user: req.user.userId }, parsed.data, {...})` -> `Address.findOneAndUpdate({ _id: req.params.id, user: req.user.userId }, parsed.data, {...})`. Same change in `deleteAddress`: `findByIdAndDelete` -> `findOneAndDelete` |
| 6 | `addressController.ts` | `ERR_HTTP_HEADERS_SENT: Cannot set headers after they are sent` | In `deleteAddress`, added `return` as its own line immediately after `res.status(404).json({ error: "Address not found" })` |
| 7 | `orderController.ts` | Client-supplied `price` used to calculate `totalAmount` | Removed `price` from `createOrderSchema`'s item shape; replaced `totalAmount += item.price * item.quantity` with `totalAmount += updatedProduct.price * item.quantity` (using the atomically-fetched DB document) |
| 8 | `orderController.ts` | No check that `address` belongs to `req.user.userId` | Added, before the stock-decrement loop: `const addressDoc = await Address.findOne({ _id: address, user: req.user.userId }); if (!addressDoc) { return res.status(404).json({ error: "Address not found" }); }` |
| 9 | `addressController.ts` | Multiple addresses could have `isDefault: true` at once | Added, before `Address.create(...)` in `createAddress`: `if (isDefault) { await Address.updateMany({ user: req.user.userId, isDefault: true }, { $set: { isDefault: false } }); }` |
| 10 | `server.ts` / `app.ts` | `Cannot POST /api/addresses`, `Cannot POST /api/orders` | Added `import addressRoutes from "./routes/addressRoutes"` + `app.use("/api/addresses", addressRoutes)`, and the equivalent two lines for `orderRoutes` |
| 11 | `server.ts` | `GET /api/coupons` returned category data | `import couponRoutes from "./routes/categoryRoutes"` -> `import couponRoutes from "./routes/couponRoutes"` |
| 12 | `orderRoutes.ts` | `GET /api/orders/my-orders` -> 404 | `router.post("/my-orders", verifyToken, getMyOrders)` -> `router.get("/my-orders", verifyToken, getMyOrders)` |
| 13 | `productRoutes.ts` | Duplicate unreachable route | Deleted the second, repeated line `router.post("/", verifyToken, requireAdmin, upload.array("images", 5), createProduct)` |
| 14 | `app.ts` | `TypeError: The "data" argument must be of type string or Buffer... Received an instance of Object` (webhook HMAC check) | Moved the line `app.use("/api/webhooks", webhookRoutes)` to appear before `app.use(express.json())`, instead of after it |
| 15 | `webhookController.ts` | Valid webhook payloads rejected as `"Invalid webhook payload"` | `if (eventId || eventType) { res.status(400)...; return; }` -> `if (!eventId || !eventType) { res.status(400)...; return; }` |
| 16 | `webhookController.ts` | Non-duplicate DB errors silently ignored, request hung with no response | In the `catch (err)` block after `WebhookEvent.create(...)`, added `throw err;` as the last line, after the `if (err.code === 11000)` check |
| 17 | `categoryController.ts` | Response body had key `err0r` instead of `error` | `res.status(404).json({ err0r: "Category not found" })` -> `res.status(404).json({ error: "Category not found" })` |
| 18 | `__tests__/coupons.test.ts` | Test expected `409`, got `400` for maxed-out coupon | `expect(res.status).toBe(409);` -> `expect(res.status).toBe(400);` in the `"enforces the coupon usage limit"` test |
| 19 | `validators/orderValidator.ts` | `400: "Invalid input: expected string, received undefined"` | `idempotencyKey: z.string().min(1, "Idempotency key is required")` -> `idempotencyKey: z.string().min(1).optional()`; added new line `couponCode: z.string().min(1).optional(),` |
| 20 | `couponController.ts`, `orderController.ts` | `No overload matches this call... Type 'undefined' is not assignable to type 'number'` (`exactOptionalPropertyTypes`) | Built the object passed to `.create(...)` using conditional spreads instead of direct keys, e.g. `...(minOrderValue !== undefined && { minOrderValue }),` instead of `minOrderValue,` |
| 21 | `addressController.ts`, `categoryController.ts`, `ticketController.ts` | `Object is possibly 'undefined'` on `req.params.id` (`noUncheckedIndexedAccess`) | Added, at the top of each affected function: `const id = req.params.id; if (!id) { res.status(400).json({ error: "ID is required" }); return; }` — then used `id` instead of `req.params.id` for the rest of the function |
| 22 | `tsconfig.json` | `TS5107: Option 'moduleResolution=node10' is deprecated` | Added new line inside `compilerOptions`: `"ignoreDeprecations": "6.0",` |
| 23 | `__tests__/setup.ts` | `Cannot find name 'beforeAll'` | In `tsconfig.json`, changed `"types": ["node"]` to `"types": ["node", "jest"]` |
| 24 | `__tests__/setup.ts` | `Object is possibly 'undefined'` on `collections[key]` | `await collections[key].deleteMany({});` -> `await collections[key]!.deleteMany({});` |
| 25 | `__tests__/auth.test.ts`, `products.test.ts`, `webhook.test.ts` | `` `key_id` or `oauthToken` is mandatory `` (Razorpay SDK, on `import app`) | Added at the top of each file: `jest.mock("razorpay", () => jest.fn().mockImplementation(() => ({ orders: { create: jest.fn().mockResolvedValue({ id: "order_test_mock123" }) } })));` |
| 26 | `__tests__/auth.test.ts` | Register/login tests failing with `400`/`401` unexpectedly | Test payload changed from `{ name, email, password: "password123" }` to `{ name, email, password: "Passw0rd!", phone: "9876543210" }` |
| 27 | `config/logger.ts` | Same `exactOptionalPropertyTypes` error as #20, on `transport` option | Replaced one `pino({ level, transport: isProd ? undefined : {...} })` call with two branches: `isProduction ? pino({ level }) : pino({ level, transport: {...} })` |
| 28 | `config/db.ts` | Logged literal string `"error"` instead of the real error, hiding root cause | `console.error("MongoDB connection failed: error");` -> `console.error("MongoDB connection failed:", error);` |
| 29 | `.github/workflows/ci.yml` | Same Razorpay SDK crash as #25, in CI | Added two lines under the `Run tests` step's `env:` block: `RAZORPAY_KEY_ID: rzp_test_dummy_key_for_ci` and `RAZORPAY_KEY_SECRET: dummy_secret_for_ci` |
| 30 | `.github/workflows/ci.yml` | Old broken `Run tests` step still present, ran before the corrected one | Deleted the first (old) `- name: Run tests` block, keeping only the one with all four env vars |
| 31 | `.github/workflows/ci.yml` | Pushes to `dev` never triggered CI | Under `on: push:`, changed `branches: [main]` to `branches: [main, dev]` |
| 32 | `.github/workflows/ci.yml` | `Redis client error ECONNREFUSED 127.0.0.1:6379` in CI | Added a `services:` block under `jobs: test:`: `redis: image: redis:7, ports: ["6379:6379"]` with a health-check |
| 33 | `.github/workflows/ci.yml` | `Error: No event triggers defined in 'on'` | Restored the missing top of the file: `name: CI` followed by the full `on: push:/pull_request:` block, which had been accidentally deleted |
| 34 | `config/redisClient.ts` | Redis unreachable from inside Docker container | `host: "127.0.0.1", port: 6379` -> `host: process.env.REDIS_HOST || "127.0.0.1", port: Number(process.env.REDIS_PORT) || 6379`; set `REDIS_HOST=redis` in `docker-compose.yml`'s `app` service |
| 35 | `scripts/seed.js` | Seeded data invisible via API despite "success" message | Ran `$env:MONGO_URI="mongodb://localhost:27017/storix"` before `node scripts/seed.js` so the seed script targeted the same Mongo container the Dockerized server was using, instead of the Atlas URI from `.env` |
| 36 | N/A (Docker Compose usage) | Containers stopped unexpectedly when the terminal was closed | `docker-compose up` -> `docker-compose up -d` |
| 37 | Render dashboard -> Environment tab | `` `key_id` or `oauthToken` is mandatory `` in production | Added all missing key/value pairs: `MONGO_URI`, `JWT_SECRET`, `CLOUDINARY_CLOUD_NAME`, `CLOUDINARY_API_KEY`, `CLOUDINARY_API_SECRET`, `RAZORPAY_KEY_ID`, `RAZORPAY_KEY_SECRET`, `RAZORPAY_WEBHOOK_SECRET`, `REDIS_URL`, `PORT=5000`, `NODE_ENV=production` |
| 38 | Render env var `REDIS_URL` | `Redis client error EPIPE` / `MaxRetriesPerRequestError` in production | Value changed from `redis://default:<pwd>@<host>.upstash.io:6379` to `rediss://default:<pwd>@<host>.upstash.io:6379` (added the second `s`); also added `{ maxRetriesPerRequest: 3, enableReadyCheck: false, retryStrategy(times) { return Math.min(times * 100, 2000); } }` as the second argument to `new Redis(process.env.REDIS_URL, {...})` |
| 39 | MongoDB Atlas -> Network Access -> IP Access List | `MongooseServerSelectionError: Could not connect to any servers in your MongoDB Atlas cluster` | Clicked "+ ADD IP ADDRESS" -> "Allow Access from Anywhere", adding `0.0.0.0/0` as a new entry alongside the existing specific IPs |
